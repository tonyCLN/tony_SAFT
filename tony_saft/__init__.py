name = "example_pkg"

cheat = '''
1. autoreload - for prototyping:

%load_ext autoreload
%autoreload 2

import example_pkg
from example_pkg import *

2. restart kernel and run all cells - for validation:

"For the latest jupyter notebook, (version 5) you can go to the 'help' tab in the top of the notebook and then select the option 'edit keyboard shortcuts' and add in your own customized shortcut for the 'run all' function."

restart kernel and run all cells > Ctrl-Shift-Enter

'''
